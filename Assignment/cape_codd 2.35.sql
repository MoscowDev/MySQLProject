/*
2.35 Explain the difference between the SQL built-in functions COUNT and SUM.

COUNT: Returns the number of rows that match a specified condition. 
       It can count all rows (COUNT(*)) or only non-NULL values in a column (COUNT(column)).

SUM: Returns the total sum of a numeric column for all rows that match a specified condition. 
     It only works on numeric data and ignores NULL values.
*/
